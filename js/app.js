window.onload = function() {
    renderHome();
    renderUpdates();
    renderCodes();
    renderMaps();
    renderStaff();
    renderClips();
    renderAssets();

    init3DTilt();

    handleInitialRoute(true);
};
